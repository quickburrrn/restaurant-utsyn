image: node:20

stages:
  - build
  - deploy

variables:
  NODE_ENV: production

build:
  stage: build
  script:
    - npm install
    - npm run build
    - ls

deploy:
  stage: deploy
  script:
    - hello world